spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chilink?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: chilink
    password: chilink

  jpa:
    hibernate:
      ddl-auto: validate # update 안 씀
